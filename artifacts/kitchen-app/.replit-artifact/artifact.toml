kind = "mobile"
previewPath = "/"
title = "PrepFlows"
version = "1.0.0"
id = "artifacts/kitchen-app"
router = "expo-domain"

[[integratedSkills]]
name = "expo"
version = "1.0.0"

[[services]]
ensurePreviewReachable = "/status"
name = "expo"
paths = [ "/" ]
localPort = 25160

[services.development]
run = "pnpm --filter @workspace/kitchen-app run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/kitchen-app", "run", "build" ]
run = [ "pnpm", "--filter", "@workspace/kitchen-app", "run", "serve" ]

[services.env]
PORT = "25160"
BASE_PATH = "/"
